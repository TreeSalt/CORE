TRADER_OPS — MASTER IDE REQUEST
Target: v4.5.271
PROMPT_ID: TRADER_OPS_MASTER_IDE_REQUEST_v4.5.271_IBKR_GATEWAY_AUTOMATION_FAIL_CLOSED
CHARTER_ID: TRADER_OPS_PROMPT_CHARTER_v1.0
Mission: “IBKR Gateway Automation (Start/Wait/Ingest) — No Secrets, Fail-Closed, One Command”
Base: v4.5.270 must pass strict verification before edits.

NON-NEGOTIABLE RULES
- FAIL CLOSED. No silent fallbacks.
- No secrets in repo/logs/artifacts/evidence (no credentials, tokens, account IDs).
- Allowed file list is mandatory. If you need to touch a file not listed: STOP and report.
- Paste raw outputs for every acceptance command.

ALLOWED FILES
- scripts/*
- Makefile
- docs/*

PRE-FLIGHT (paste raw output)
1) python scripts/verify_drop_packet.py --strict --drop dist/TRADER_OPS_READY_TO_DROP_v4.5.270.zip --run-ledger dist/RUN_LEDGER_v4.5.270.json --trusted-pubkey keys/sovereign.pub

PATCH TASKS
P0) Add IBKR Gateway supervisor script (start + wait + fail closed)
- Add: scripts/ibkr_gateway_supervisor.sh
- Behavior:
  - “up”: starts IB Gateway if not already running (path configurable via env var IBGATEWAY_BIN, default /home/AWS/.local/opt/ibgateway/ibgateway)
  - “wait”: waits for API port to listen (default 4002), with timeout and clear failure message
  - Must not print secrets; must not store credentials
  - Must exit nonzero on failure
  - Must print a single actionable line if login is required (“Gateway launched; please complete login/2FA; waiting for port …”)

P0-B) Makefile targets
- make ibkr-up     => scripts/ibkr_gateway_supervisor.sh up --port 4002
- make ibkr-wait   => scripts/ibkr_gateway_supervisor.sh wait --port 4002 --timeout 90
- make ibkr-ingest-30d => (ibkr-up then ibkr-wait then run v9e_stage/scripts/ibkr_ingest_mes_5m.py)

P1) Docs: one-page “Reality Contact” checklist
- Add docs/IBKR_GATEWAY_AUTOMATION.md with:
  - one-time UI steps to enable API + set port
  - commands to run ibkr-up/wait/ingest
  - troubleshooting table (port closed, wrong port, gateway not running)

ACCEPTANCE CRITERIA (paste raw output)
A) make ibkr-up launches gateway (or detects already running) without error
B) make ibkr-wait fails if gateway not logged in / port not open within timeout (fail closed)
C) After manual login, make ibkr-wait succeeds and shows the port is open
D) make ibkr-ingest-30d runs and writes the expected output files (paste ls output)

DEFINITION OF DONE
- One command launches gateway (if needed), waits for API readiness, and ingests bars without human glue.
- Manual login remains the only human step, and the system communicates that clearly.
- No secrets stored anywhere in repo or logs.
