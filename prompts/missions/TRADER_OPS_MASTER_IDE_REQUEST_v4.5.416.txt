PROMPT_ID: TRADER_OPS_HOTFIX_v4.5.416_ORCHESTRATION_GUARDS
CHARTER: TRADER_OPS_PROMPT_CHARTER_v2.0
SCOPE: P0 (Orchestration sequencing fix — allowlist gate + offline adapter enforcement)

MISSION:
Fix sequencing so smoke/backtest runs are strictly offline and fail closed before engine init when allowlist is violated.

NON-NEGOTIABLE RULES:
- Smoke/backtest MUST NOT attempt network connections or initialize IBKR live/paper adapters.
- Allowlist must be evaluated BEFORE engine initialization.
- If allowlist violated: write results.csv status=SMOKE_BLOCKED_ALLOWLIST_VIOLATION, write NO_TRADE_REPORT.json, write header-only fill_tape.csv, then exit (no engine boot).

ALLOWED FILES (STOP if more needed):
- scripts/generate_evidence.py
- antigravity_harness/cli.py
- antigravity_harness/portfolio_engine.py (only if adapter selection lives here)
- antigravity_harness/reporting.py (only to emit status/artifacts)
- Makefile

ORDER:
1) PLAN: identify where adapter is chosen + where CAPABILITY_SNAPSHOT is loaded.
2) PATCH A: early allowlist gate in generate_evidence.py or CLI entrypoint — exit before engine init.
3) PATCH B: hard-force adapter="backtest" for smoke runs; forbid network adapter init under smoke/backtest.
4) TEST: run smoke and prove:
   - no socket attempts
   - if symbol not in allowlist → blocked status + NO_TRADE_REPORT + fill_tape header only
5) VERIFY: make verify strict pass.

ACCEPTANCE:
A) Running smoke produces zero network connection attempts (no 127.0.0.1:9999).
B) If allowlist excludes selected symbol: status=SMOKE_BLOCKED_ALLOWLIST_VIOLATION and engine is not initialized.
C) Evidence directory contains NO_TRADE_REPORT.json and fill_tape.csv (header-only).
D) make verify PASS.
