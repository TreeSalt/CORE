TRADER_OPS — MASTER IDE REQUEST
Target: v4.5.134
PROMPT_ID: TRADER_OPS_MASTER_IDE_REQUEST_v4.5.134_AUTOPILOT_SAFETY_SUPERVISOR_V2
CHARTER_ID: TRADER_OPS_PROMPT_CHARTER_v1.0
Mission: “Autopilot Safety Supervisor v2 (Fail-Closed Verify + Paper-Run Guardrails + Council Packet Artifact)”
Base: v4.5.133 must pass strict verification before edits.

NON-NEGOTIABLE RULES
- FAIL CLOSED. No silent fallbacks. Supervisor must refuse to proceed on any failed check.
- Allowed file list is mandatory. If you need to touch a file not listed: STOP and report.
- No secrets in repo, logs, artifacts, or evidence (API keys, tokens, account IDs, etc).
- Supervisor is NOT allowed to place live trades unless explicitly enabled and certified.
- Supervisor must reduce human “pony-express” errors: one command to run the correct sequence.
- Paste raw outputs for every acceptance command.

ALLOWED FILES
- scripts/*
- Makefile
- antigravity_harness/cli.py (wire-in ok)
- antigravity_harness/strategies/registry.py (queries ok)
- docs/*
- keys/*

BASELINE ARTIFACTS (do not edit)
- DROP sha256:
  90ad8b71680c86117d006a8d4e785f3f026a29ddf1d3640465bb7fd2f3bfa69e  TRADER_OPS_READY_TO_DROP_v4.5.133.zip
- RUN_LEDGER (raw JSON):
{
  "artifacts": {
    "code": {
      "filename": "TRADER_OPS_CODE_v4.5.133.zip",
      "sha256": "b707bec106e689262e08bd8d08fde440d34cc585cef47c04e5d493e22c0c5928"
    },
    "evidence": {
      "filename": "TRADER_OPS_EVIDENCE_v4.5.133.zip",
      "sha256": "cd5e872b753bdcd9f336a2ccf007e7690e849ab1ce602c3d72266c638f24478e"
    },
    "ready_to_drop": {
      "filename": "TRADER_OPS_READY_TO_DROP_v4.5.133.zip",
      "sha256": "90ad8b71680c86117d006a8d4e785f3f026a29ddf1d3640465bb7fd2f3bfa69e"
    }
  },
  "build_wallclock_utc": "2026-02-20T04:01:13Z",
  "sovereign_binding": {
    "builder_id": "Institutional-Gold-Node",
    "data_hash": "ab2abe7da8bee399d9ec0e8b4ea5887f76404821edaec19307b08d6d4d22611f",
    "drop_preimage_sha256": "16e1e5f4d6501a8d1c2c4b2b7d5b4f1d3c1b5c8449c5d5d7a03c31d882c7fda2",
    "git_commit": "120e4c4687ae40fa025d63f0c80f2b0ad7f20577",
    "git_dirty": false,
    "manifest_sha256": "a90d8ddc8dc30d4631f29399ad0c360d2cc6d19f2f83e8434d57d945f1d2df41",
    "payload_manifest_sha256": "1ad2b44cc8a21918c4e33ee1f0ef3a9b2a7c495c88f2a9f2c5d7a3b1d0c9e5aa",
    "evidence_manifest_sha256": "f1d2c3b4a5e6f708192a3b4c5d6e7f8091a2b3c4d5e6f708192a3b4c5d6e7f80"
  },
  "strict_mode": true,
  "timestamp_utc": "2026-02-20T04:01:13Z",
  "version": "4.5.133"
}

PRE-FLIGHT (paste raw output)
1) python scripts/verify_drop_packet.py --strict --drop dist/TRADER_OPS_READY_TO_DROP_v4.5.133.zip --run-ledger dist/RUN_LEDGER_v4.5.133.json --trusted-pubkey keys/sovereign.pub
2) Confirm PASS before modifying anything. If it FAILS for ANY reason: STOP and report.

PATCH TASKS

P0) Autopilot Safety Supervisor v2 (fix import, enforce trust root, enforce paper guardrails)
- File: scripts/autopilot_supervisor.py
- REQUIRED FIX: if sys.path/REPO_ROOT setup occurs after antigravity_harness imports, move sys.path setup ABOVE all internal imports so the script can run from repo root without ModuleNotFoundError.
- Behavior (single entrypoint):
  - autopilot_supervisor.py verify --dist dist --trusted-pubkey keys/sovereign.pub
    1) FAIL CLOSED if keys/sovereign.pub missing OR sha256 mismatch against pinned expected value (strict trust root).
    2) Auto-select newest READY_TO_DROP zip in dist/ (by semantic version in filename). Derive expected vX.Y.Z for all companion artifacts:
       - TRADER_OPS_CODE_vX.Y.Z.zip
       - TRADER_OPS_EVIDENCE_vX.Y.Z.zip
       - RUN_LEDGER_vX.Y.Z.json + .sig
       - DROP_PACKET_SHA256_vX.Y.Z.txt
    3) Run strict verify chain (must be the real one, not a reduced audit):
       - python scripts/verify_drop_packet.py --strict --drop <ready_to_drop> --run-ledger <run_ledger> --trusted-pubkey <trusted_pubkey>
       - python scripts/verify_run_ledger_signature.py --run-ledger <run_ledger> --sig <run_ledger_sig> --trusted-pubkey <trusted_pubkey>
       - python scripts/verify_certificate.py --evidence <evidence_zip> --trusted-pubkey <trusted_pubkey>
    4) Verify “prompt provenance” from evidence (FAIL CLOSED):
       - Read reports/certification/PROMPT_FINGERPRINT.json FROM EVIDENCE ZIP (do NOT rely on working-tree copies).
       - Extract prompt_id + prompt_sha256.
       - Require a local prompt source file exists in repo (choose ONE deterministic convention; document it), e.g.:
         docs/prompts/<prompt_id>.txt
       - sha256(local prompt bytes) MUST equal prompt_sha256 from fingerprint.
    5) Verify data leaf hashes match shipped dataset bytes (FAIL CLOSED):
       - Read reports/certification/DATA_MANIFEST.json FROM EVIDENCE ZIP.
       - For each dataset entry in the manifest (leaf files), compute sha256 over the corresponding dataset bytes as shipped (inside EVIDENCE ZIP if that’s where data lives).
       - Every leaf sha must match; recompute and match the declared data_hash/merkle root if present.
    6) Gate report truth (FAIL CLOSED):
       - Require dist/gate_report.json exists for the selected version.
       - Validate it contains required strict gates (at minimum):
         - pubkey pin verification
         - certificate signature verification
         - run ledger signature verification
         - manifest integrity (full file list)
         - timeline sovereignty / sidecar binding
       - If gate_report is missing or missing required gates: FAIL.
  - autopilot_supervisor.py run-paper --strategy <id> --profile profiles/seed_profile.yaml --dist dist --trusted-pubkey keys/sovereign.pub
    1) Runs verify mode above first (mandatory).
    2) Strategy governance checks (FAIL CLOSED):
       - strategy id is registered (via antigravity_harness/strategies/registry.py)
       - strategy tier is allowed for paper
       - if “certified” state exists, require certified for paper unless the governance rules explicitly allow lab/quarantine (default: certified only)
    3) Enforce seed_profile safety constraints (minimum FAIL CLOSED):
       - live_trading_enabled must be false for paper
       - daily_loss_cap_usd present and numeric
       - max_contracts present and numeric
       - news_blackout handling consistent with profile flags (no “news trading” if disabled)
    4) Only then calls the existing CLI paper runner path (antigravity_harness/cli.py), OR prints “ready to run” if wiring is not present yet.
  - Every failure must be a clear nonzero exit + short actionable message (what failed + how to fix).

P0-B) Make it first-class Make targets (reduce pony-express)
- Update Makefile:
  - make autopilot-verify  => python scripts/autopilot_supervisor.py verify --dist dist --trusted-pubkey keys/sovereign.pub
  - make autopilot-paper   => python scripts/autopilot_supervisor.py run-paper --strategy <id> --profile profiles/seed_profile.yaml --dist dist --trusted-pubkey keys/sovereign.pub

P0-C) Trusted pubkey distribution (repo default path must work)
- Add: keys/sovereign.pub
- Requirement:
  - autopilot verify MUST default to keys/sovereign.pub if --trusted-pubkey not provided.
  - strict verification MUST FAIL if the key is missing.
  - Ensure keys/sovereign.pub is included in the code zip payload (payload manifest / one_true_command verification must cover it).

P1) Council Packet summary artifact (deterministic, no LLM)
- Add: scripts/council_packet.py (or extend autopilot supervisor with a subcommand)
- Behavior:
  - council_packet.py emit --dist dist
    - Auto-select newest version vX.Y.Z in dist/ (same selection logic as supervisor)
    - Emit: dist/COUNCIL_BRIEF_vX.Y.Z.md including:
      - artifact shas (code/evidence/ready_to_drop/drop_preimage)
      - strict gate_report summary (pass/fail + required gates list)
      - prompt_id + prompt_sha256 (from PROMPT_FINGERPRINT in evidence zip)
      - data_hash + dataset files list (from DATA_MANIFEST in evidence zip)
- Critical fix:
  - Read PROMPT_FINGERPRINT.json / DATA_MANIFEST.json / results.csv FROM EVIDENCE ZIP (not repo_root/reports paths), so COUNCIL_BRIEF never shows “N/A” when evidence exists.
- This does NOT contact any LLMs (no OpenClaw yet). Deterministic file-to-file only.

ACCEPTANCE CRITERIA (paste raw output)
A) Pre-flight strict verify PASS (v4.5.133):
   python scripts/verify_drop_packet.py --strict --drop dist/TRADER_OPS_READY_TO_DROP_v4.5.133.zip --run-ledger dist/RUN_LEDGER_v4.5.133.json --trusted-pubkey keys/sovereign.pub

B) Autopilot verify succeeds on v4.5.134 dist artifacts:
   python scripts/autopilot_supervisor.py verify --dist dist --trusted-pubkey keys/sovereign.pub

C) Autopilot verify FAIL-CLOSED on 1-byte corruption:
   1) Copy: dist/TRADER_OPS_READY_TO_DROP_v4.5.134.zip -> dist/TRADER_OPS_READY_TO_DROP_v4.5.134_CORRUPT.zip
   2) Corrupt exactly 1 byte in the copy
   3) Run:
      python scripts/autopilot_supervisor.py verify --dist dist --trusted-pubkey keys/sovereign.pub
   Must FAIL with a short actionable message.

D) Autopilot paper refuses unsafe runs (FAIL CLOSED):
   1) Unregistered strategy:
      python scripts/autopilot_supervisor.py run-paper --strategy DOES_NOT_EXIST --profile profiles/seed_profile.yaml --dist dist --trusted-pubkey keys/sovereign.pub
   2) Non-certified strategy (choose a known non-certified id if your registry has tiers):
      python scripts/autopilot_supervisor.py run-paper --strategy <KNOWN_NONCERT_ID> --profile profiles/seed_profile.yaml --dist dist --trusted-pubkey keys/sovereign.pub
   3) Missing seed_profile fields (temporarily point to a broken profile file):
      python scripts/autopilot_supervisor.py run-paper --strategy <VALID_ID> --profile profiles/seed_profile_BROKEN.yaml --dist dist --trusted-pubkey keys/sovereign.pub
   Each must FAIL with a short actionable message.

E) Make targets work:
   make autopilot-verify
   make autopilot-paper STRATEGY=<VALID_ID>

F) Council brief generation works and is fully populated (no N/A when evidence exists):
   python scripts/council_packet.py emit --dist dist
   Then show the generated file exists:
   ls -la dist/COUNCIL_BRIEF_v4.5.134.md
   And paste the top summary section showing:
   - artifact shas
   - gate_report summary
   - prompt_id + prompt sha
   - data_hash + dataset file list

DEFINITION OF DONE
- One command produces the correct strict verify chain without human glue (autopilot-verify).
- One command refuses unsafe paper runs unless governance + safety pass (autopilot-paper).
- Council brief is emitted as a deterministic artifact populated from EVIDENCE ZIP (no pony-express assembly).
- Trust root is explicit and fail-closed: verification cannot succeed without a valid trusted pubkey.
